{
    "name": "action-char-count",
    "version": "1.0.0",
    "description": "A GitHub Action to count characters in a string.",
    "main": "index.js",
    "scripts": {
        "build": "ncc build index.js -o dist"
    },
    "license": "MIT",
    "dependencies": {
        "@actions/core": "^1.10.0",
        "@actions/github": "^5.0.0",
        "@actions/glob": "^0.4.0"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.38.0"
    }
}
